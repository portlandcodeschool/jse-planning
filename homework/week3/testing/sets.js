
var sets = require('../solution3-sets.js');

module.exports = {
    union:      sets.union,
    intersection: sets.intersection,
    difference: sets.difference,
    copy:       sets.copy,
    equal:      sets.equal,
    similar:    sets.similar
}
