var array = {length:0};

array.pop = function() { //changes array, returns ??
	//...
}

array.push = function(/*...*/) { //changes array, returns a number
	//...
}

array.join = function(/*...*/) { //returns a string
	//...
}
